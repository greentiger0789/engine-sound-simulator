# Project-scoped fallbacks for unspecified children; select each ticket worker explicitly.
# Keep the parent model and permission policy unchanged.
[agents]
enabled = true
max_concurrent_threads_per_session = 3
default_subagent_model = "gpt-6-sol"
default_subagent_reasoning_effort = "medium"
