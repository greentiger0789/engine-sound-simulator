# Ticket 4 — implementation record

## Status

Status: verified

## Scope

- Ticket: [音声ライフサイクルの回帰テストと CI 基準](../004-test-and-ci-baseline.md)
- Reviewed implementation commits: `a9c5fe818608814d71a82136801fec04a6f58ba4`
  and correction `a5cfa629119b4aafde46faab073e74b5ab1e6d28`
- Extracted the controller's browser-independent lifecycle transitions and
  stable user-facing mappings for module-load and processor failures.
- Extended the product E2E suite with repeated-start, recoverable Worklet-load
  failure, processor failure, restart, and React development StrictMode
  remount coverage.
- Retained the existing `Repository checks` aggregation, Dockerized commands,
  and the matching Playwright 1.63.0 package/image pins because they already
  satisfy this ticket's CI and Chromium reproducibility contracts.

## Acceptance evidence

- `make test` passed 5 Vitest files / 24 tests. The six pure lifecycle tests
  exercise the full normal transition sequence, duplicate-start rejection,
  invalid transition rejection, interruption/recovery transitions, and both
  required displayable error mappings. Existing controller tests continue to
  cover stop during module loading and graph lifecycle behavior.
- `make e2e` passed 12 Playwright cases across Vite development and Nginx
  production projects. The real product UI cases verify user-gesture start and
  Worklet `ready`, rapid duplicate start without duplicate graphs, stop and
  restart, injected module-load failure with a typed alert and successful
  retry, and injected `processorerror` with graph recreation on retry.
- The Vite development project executes the product after React StrictMode's
  development mount/cleanup/remount cycle. The test observes the development
  lifecycle diagnostics (`mount=2`, `cleanup=1`) before verifying start, stop,
  and restart without duplicate graphs. The Nginx project verifies the same
  production restart/deduplication contract without claiming a development-only
  StrictMode remount.
- `make check` passed Prettier, Markdownlint, ticket tooling tests, ESLint, and
  strict TypeScript compilation. `make e2e` also rebuilt the production app and
  Nginx E2E image successfully.
- `.github/workflows/ci.yml` retains the required `Repository checks` job and
  runs `make ci`. That target aggregates check/type/lint, unit, build, E2E,
  actionlint, Hadolint, and committed-history Gitleaks with failure propagation.
- `@playwright/test` and its lockfile graph remain fixed at 1.63.0, matching the
  digest-pinned `mcr.microsoft.com/playwright:v1.63.0-noble` Docker image.

## Review

- Self-review covered `origin/main...a9c5fe8`, including controller/helper
  contract agreement, asynchronous start/stop interruption, error recovery,
  graph counting, real Worklet readiness, dev/production test scope, and the
  unchanged CI/image pin contracts.
- Two bounded Terra medium `ticket-worker` agents implemented non-overlapping
  pure-helper/unit and Playwright E2E portions. The orchestrator integrated the
  helper with the controller and retained ownership of shared contracts, Git,
  documentation, and final verification.
- The independent Terra high reviewer found that the original E2E inferred but
  did not observe StrictMode's remount, and that this report's test counts became
  stale after a self-review regression test. The correction adds observable
  development-only mount/cleanup events and assertions, updates the evidence,
  and retains accurate production scope. The reviewer also confirmed the
  self-review fix for a queued stop after module-load failure. A final recheck
  found both review findings resolved and no new actionable regressions.

## Verification

- `make test` — passed after review corrections: 5 files, 24 tests.
- `make check` — passed after integration.
- `make e2e` — passed: 12 tests across Vite development and Nginx production.
- Focused controller/lifecycle rerun — passed: 2 files, 17 tests.
- `make ci` — passed after review corrections, including check/type/lint,
  24 unit tests, production builds, 12 E2E tests, actionlint, Hadolint, and
  committed-history Gitleaks.
- GitHub `Repository checks` is recorded in the Draft PR rather than this
  implementation record.

## Limits and next actions

The optional Windows Chromium/Edge listening check was not run, and no
headless result is presented as physical-device or sound-quality evidence. It
is not mandatory for ticket 4. No mandatory local checks or review findings
remain pending; the final GitHub required check is recorded in the Draft PR.
