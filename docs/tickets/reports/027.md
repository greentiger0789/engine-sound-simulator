# Ticket 27 — responsive dashboard implementation record

## Status

Status: verified

The mandatory automated acceptance, independent review and committed local Docker gate are complete.

## Scope

- Ticket: [レスポンシブ対応とPC向け横長ダッシュボード](../027-responsive-dashboard.md).
- Reviewed implementation revision: `0adbe22`.
- Reorganized the existing controls and visualizations into a two-column desktop dashboard, kept the preset and pending state visible, and moved stopped-only settings into native collapsible details.
- Narrow layouts stack the same controls without remounting the audio controller or visualization. The hold accelerator releases when a touch moves outside its bounds.

## Acceptance evidence

- `tests/e2e/dashboard.spec.ts`, `shows the full dashboard`: checks the bounding boxes of transport, volume, RPM, throttle, load, gear, clutch, speed, preset, firing strip, waveform and spectrum in both 1366×768 and 1440×900 CSS viewports. It also checks that details start closed and that neither axis overflows the page.
- `tests/e2e/dashboard.spec.ts`, `keeps expanded eight-cylinder settings usable`: at 320, 375 and 768px, expands details, edits cylinder 8, stages the V8 config and checks horizontal overflow and input reachability. The 320px viewport also covers a viewport narrower than half of 1366px for the zoom-equivalent access check.
- `tests/e2e/dashboard.spec.ts`, `preserves playback and inputs`: stages V8, starts native audio, resizes from 1440px to 320px, opens and closes details, checks running state, active configuration and volume/throttle/load values, and stops audio. Canvas backing stores are checked at initial idle, running resize and stopped resize against both CSS dimensions and device pixel ratio. Code inspection confirms the existing controller is stored outside React and no resize/toggle path creates a new context; the E2E does not directly count context instances in this case.
- `tests/e2e/dashboard.spec.ts`, touch-outside case: checks hold release after moving the active touch outside the accelerator.
- Existing `tests/e2e/worklet.spec.ts` covers stopped-only staging, pending config, keyboard shifts, validation focus, visualization reads and restart cleanup across development and Nginx browser projects. Cases that access stopped-only fields explicitly open details.
- Screenshot evidence: [1366px desktop](../evidence/027/desktop-1366.png), [1440px desktop](../evidence/027/desktop-1440.png), [320px expanded mobile](../evidence/027/mobile-320-expanded.png). Both desktop images show readable controls, firing strip and analyser panes within the viewport; the 320px expanded form is readable without clipped inputs. These are Chromium CSS viewport captures, not physical phone evidence.

## Review

- Self-review covered the full diff against `origin/main`, narrow input overflow, keyboard focus, touch hold cancellation, validation announcements, stable controller state, visualization cleanup and screenshot readability. E2E exposed duplicate validation alerts and a Space-key hold regression, both corrected before the 50-case pass.
- A `gpt-6-sol` / medium worker owned `src/app/App.tsx` and `src/styles/index.css` because the UI layout, existing control state and keyboard/pointer behavior interact. Its selected and observed model/effort matched. The orchestrator owns E2E, documentation, Git and integration.
- A fresh independent `ticket-reviewer` (`gpt-6-sol` / high) found that canvas backing stores were resized only while running. The sizing effect now remains active at idle and after stop, independent from the RAF animation loop. The reviewer rechecked the corrected effect and new initial/running/stopped E2E assertions and reported no remaining actionable issue. The reviewer inspected all three screenshots independently.

## Verification

- Docker `make e2e` — passed: 50 Chromium tests across Vite development and Nginx production projects, including the review correction.
- Docker `make ci` — passed on committed implementation revision `0adbe22`: formatting, Markdown, ticket catalog/tooling, ESLint, TypeScript, 193 unit tests in 25 files, application and web image builds, 50 Playwright cases, actionlint, Hadolint and committed-history Gitleaks over 108 commits.
- Final-head GitHub `Repository checks`: pending until the Draft PR is pushed.

## Limits and next actions

- Smartphone physical-device audio and listening are optional for this ticket and were not performed. No physical-device result is claimed.
- No mandatory local acceptance check or review finding remains pending. Current-head GitHub CI is tracked in the Draft PR.
