# Ticket 26 — implementation record

## Status

Status: blocked

## Scope

- Ticket: [Node.js 26 / TypeScript 7 ツールチェーン移行](../026-node26-typescript7-toolchain.md)
- Implementation did not start. No dependency, application, Docker, CI, lockfile,
  or configuration change was made; this record documents the unmet external
  start conditions only.
- Dependency 4 is ready: `origin/main:docs/tickets/reports/004.md` is
  `Status: verified`; both cited implementation commits
  (`a9c5fe818608814d71a82136801fec04a6f58ba4` and
  `a5cfa629119b4aafde46faab073e74b5ab1e6d28`) are ancestors of `origin/main`,
  and its cited lifecycle/unit/E2E implementation paths remain there.

## Acceptance evidence

- Node.js gate — **not met** as of 2026-09-21. The official
  [Node.js release table](https://nodejs.org/en/about/previous-releases) lists
  v26 as `Current` (last updated 2026-09-16) and v24 as `LTS`. The Node Release
  Working Group's official
  [schedule](https://github.com/nodejs/Release/blob/main/schedule.json) sets the
  v26 LTS transition to 2026-10-28. It was therefore not LTS at the required
  date.
- TypeScript/lint gate — **not met**. The active lint command is `eslint .` and
  the locked lint integration is `typescript-eslint@8.70.0`. Its official
  [dependency-version policy](https://typescript-eslint.io/users/dependency-versions/)
  specifies supported TypeScript `>=4.8.4 <6.1.0`, which excludes TypeScript 7.
  That page also says unsupported versions have no guarantees; no force or
  legacy-peer-deps workaround was used. The repository lockfile independently
  records the same `>=4.8.4 <6.1.0` peer range for the installed
  `@typescript-eslint/*` packages.
- Reproducible Docker/npm baseline — `docker compose run --build --rm tools sh
-c 'node --version; npm --version; npm ls --depth=0 typescript
typescript-eslint eslint'` completed successfully on 2026-09-21 and reported
  Node `v24.21.0`, npm `11.19.0`, `typescript@6.0.3`,
  `typescript-eslint@8.70.0`, and `eslint@10.10.0`. This confirms the existing
  locked toolchain; it is not evidence of TypeScript 7 compatibility.
- As the two mandatory upstream gates fail, the remaining migration acceptance
  criteria (Node 26 image/metadata/type alignment, clean TypeScript 7 install,
  typecheck/lint/tests/build/E2E, and documentation alignment) are pending and
  were not attempted.

## Review

- Self-review covered the complete `origin/main` delivery diff, dependency 4,
  both official start conditions, catalog/DAG consistency, and the absence of
  dependency, application, Docker, lockfile, and CI changes. It found that the
  migration must not start while either gate remains unmet.
- A fresh Terra high `ticket-reviewer` independently verified the blocked
  conclusion and found three report issues: `make check` was still listed as
  not run, the completed review was not recorded, and the exact Node 26 LTS
  date was missing. This revision records the successful check, review scope,
  and official 2026-10-28 schedule. No actionable finding remains in the
  blocked delivery.
- Recheck the two official upstream conditions before unblocking; then review
  the eventual integrated dependency, Docker, metadata, lint, lockfile, and
  documentation diff under this ticket's acceptance criteria.

## Verification

- Passed: the Docker/npm baseline command above. The image build reused its
  successful cached `npm ci --no-audit --no-fund` layer from the committed
  lockfile.
- `make check` — passed on the blocked delivery tree after report formatting;
  this validates Prettier, Markdownlint, ticket catalog/tooling tests, the
  unchanged ESLint path, and TypeScript compilation. It is not evidence of a
  Node 26/TypeScript 7 migration.
- `make ci` — passed on committed blocked-delivery revision `ccaa85f`. It
  included the checks above, 168 unit tests, production builds, 30 Playwright
  tests across development and Nginx projects, actionlint, Hadolint, and
  committed-history Gitleaks over 88 commits. These results verify the unchanged
  repository gate and blocked record; no unimplemented Node 26/TypeScript 7
  behavior is represented as successful.

## Limits and next actions

- Blocked until Node 26 is officially LTS and the selected/current lint path
  officially includes TypeScript 7 in its support range. Do not bypass either
  condition with `--force`, `--legacy-peer-deps`, peer-warning suppression, or
  reduced lint/type-check coverage.
- After both conditions are met, select compatible lint dependencies, regenerate
  the lockfile with the chosen Node 26/npm image, and run the ticket's required
  Docker checks and independent review.
- Optional after implementation: on Windows Chromium/Edge, manually confirm
  dev/web start and stop plus the principal audio controls. It is not a
  substitute for the blocked automated migration acceptance criteria.
