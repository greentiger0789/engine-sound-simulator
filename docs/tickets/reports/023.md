# Ticket 23 — V型・自動車向け6/8気筒設定 implementation record

## Status

Status: manual-validation-pending

Automated acceptance is complete except for the committed Docker CI gate. Windows listening and CPU observations are optional for this ticket.

## Scope

- Ticket: [V型・自動車向け6/8気筒設定](../023-v-and-automotive-presets.md).
- Implementation revision: pending commit.
- Added synthetic even-fire V6/V8 configuration presets with explicit firing angles, cylinder IDs and left/right bank IDs. The AudioWorklet and controller accept up to eight cylinders while the core validator retains a separate optional UI cap. The UI exposes these presets and their model-value limitation.

## Acceptance evidence

- `tests/engine/automotive-presets.test.ts` checks V6/V8 event counts, chronological order, wrap boundary, identities and bank assignments, and parseability.
- `tests/audio/ticket23-high-cylinder.test.ts` runs both presets through the product Worklet at 44.1 and 48 kHz and checks both bank buses receive events, PCM remains finite, peak is at most one, and no fatal error occurs. Its 48 kHz / 128-frame p99 matrix measures the existing four-cylinder, V6 and V8 configurations at 98% of each redline against the half-block target. The unchanged `tests/audio/ticket17-aliasing.test.ts` separately protects the four-cylinder two-bank near-redline regression.
- `tests/e2e/worklet.spec.ts` selects, stages and starts each high-cylinder preset in the actual browser product path and checks the active configuration and firing visualization. The full E2E suite passed in Vite and Nginx: 34 browser tests.
- `make test`: 24 files / 185 tests passed on the integrated working tree. The subsequent near-redline performance-test adjustment passed its focused Docker rerun (5 tests).

## Review

- Self-review covers preset constants and support envelope, event identity and bank routing, audio protection and performance harness, controller and Worklet caps, UI limits, browser E2E, and documentation. It corrected the performance test to reject a silent faulted processor and to measure near redline.
- A fresh `ticket-reviewer` (`gpt-6-sol` / high) reviewed the entire diff and reported no actionable production defects. Its evidence question about four-cylinder near-redline coverage was resolved by the existing ticket 17 test; it rechecked the later near-redline matrix correction and reported no actionable findings.
- Unresolved questions: none.

## Verification

- Preset worker's focused Docker tests: 9 passed, plus strict TypeScript, ESLint and Prettier checks.
- `make check`: passed in Docker on the integrated working tree (formatting, Markdown, catalog, tooling, lint, TypeScript).
- `make test`: passed in Docker, 24 files / 185 tests.
- Focused near-redline `ticket23-high-cylinder.test.ts`: passed in Docker, 5 tests.
- `make e2e`: passed in Docker, 34 Chromium tests across Vite development and Nginx production.
- `make ci`: pending on committed implementation revision.
- GitHub `Repository checks`: pending on final PR head.

## Limits and next actions

- These presets are synthetic model configurations, not measured vehicle or engine data. They do not derive firing order from geometry or model separate exhaust reflection and drivetrain behavior.
- Optional Windows listening, CPU and dropout observations have not been reported. No physical listening or browser profiler result is claimed.
