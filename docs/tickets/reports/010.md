# Ticket 10 — implementation record

## Status

Status: blocked

Independent review and the final delivery gate are pending.

## Scope

- Ticket: [多気筒設定と基準プリセット](../010-multicylinder-config-and-presets.md)
- Reviewed implementation commits: `808f090faa21854d5c43486be79a2546e4af9e98`
  and review correction `f94b802e8bdd0af2b288575ab7d9621ee93f3c5d`
- Added explicit synthetic 720-degree model presets for 360-, 180-, and
  270-degree parallel twins, an evenly spaced triple, and an evenly spaced
  four-cylinder configuration. Cylinder count remains derived exclusively from
  each config's `cylinders` array.
- Confirmed the existing event contract with multi-cylinder configurations,
  including cylinder identity, cycle index, within-sample offset, circular
  cycle boundaries, and stable simultaneous firing order.

## Acceptance evidence

- `tests/engine/multicylinder-presets.test.ts` checks the exact firing phases
  `0,360`, `0,180`, `0,270`, `0,240,480`, and `0,180,360,540`, plus the circular
  intervals `360/360`, `180/540`, `270/450`, `240/240/240`, and
  `180/180/180/180`, including each last-to-next-cycle boundary.
- The same test parses all five configs, asserts the 720-degree cycle and
  model-value/not-measured metadata, and verifies that no redundant
  `cylinderCount` field exists.
- A two-cycle `FiringEventGenerator` exercise checks cylinder IDs, cycle
  indices, sample offsets, and absolute event order for every preset. A
  dedicated coincident-angle case supplies IDs out of order and receives both
  events at the same frame in stable cylinder-ID order.
- A non-cycle-aligned 7-degree-per-sample triple-cylinder case analytically
  checks the next-cycle event at frame `102 + 6/7`, including cycle index,
  nonzero within-sample offset, and identical observable events after irregular
  `13/20/17/70` frame partitions.
- The direct single-cylinder regression parses the existing preset and emits
  its expected events across the 720-degree boundary. The full existing engine
  and audio unit suite also remains green.

## Review

- Self-review covered the complete `origin/main...808f090` diff against the
  required absolute phases, circular intervals, metadata, derived cylinder
  count, and existing event fields/order. It strengthened the test matrix to
  assert absolute phases independently because interval-only assertions would
  not detect a uniformly shifted preset.
- Two bounded Terra medium ticket workers implemented non-overlapping preset
  and test files; the orchestrator retained contracts, integration, Git,
  reporting, and delivery ownership.
- A fresh Terra high ticket reviewer found no implementation defect. It found
  that the original event test used only integer-aligned events, so it did not
  prove the report's within-sample offset claim. The correction adds an
  analytically checked fractional event and irregular-buffer continuity test;
  the reviewer rechecked the arithmetic and split/local index normalization,
  confirmed the finding resolved, and reported no remaining actionable
  finding.

## Verification

- `make test` — passed in Docker on `808f090`: 10 files and 74 tests.
- Focused corrected preset/event test — passed in Docker on the correction
  tree: 1 file and 6 tests.
- `make check` — passed in Docker on the same implementation tree: Prettier,
  Markdownlint, ticket/catalog tooling, ESLint, and strict TypeScript.
- `make ci` — pending until independent review is complete.
- GitHub `Repository checks` — pending until the Draft PR is pushed.

## Limits and next actions

No manual or listening evidence is mandatory for this ticket. These presets
are currently available to the browser-independent core/offline event path;
the existing product Worklet and controller intentionally remain
single-cylinder until a later integration ticket. The final local delivery
gate and current-head GitHub CI remain pending.
