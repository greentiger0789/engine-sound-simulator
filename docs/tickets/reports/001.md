# Ticket 1 — implementation record

## Status

Status: verified

## Scope

- Ticket: [React/Vite/TypeScript と Docker Web 基盤](../001-web-foundation.md)
- Reviewed implementation commit: `b8b7fe88c51d630d902d25e0ffc4234240e2606d`
- Added a React 19 StrictMode SPA entry, strict TypeScript project, ESLint with
  React Hooks rules, Vite production build, and an empty controller area without
  audio APIs.
- Added isolated Docker tooling/build/web stages, Vite and Nginx Compose
  services, loopback-only published ports, SPA fallback, and Make targets for
  development, shutdown, builds, and the expanded CI gate.
- React 19.3.0 and Vite 8.3.0 use the planned candidate versions. TypeScript
  6.0.3 is locked instead of candidate 7.0.2 because typescript-eslint 8.70.0
  declares support below TypeScript 6.1.0.

## Acceptance evidence

- `npm ci` runs in the Node 24 tooling image. `make check` passed Prettier,
  Markdownlint, ticket tooling tests, ESLint, and strict project-reference
  TypeScript compilation via `tsc --build`.
- `make build` passed the build and web image targets. The build stage produced
  the Vite `dist` output and the unprivileged Nginx image copied it into its web
  root.
- `docker compose up -d --build dev web` started Vite on the configured
  `0.0.0.0:5173` container endpoint and Nginx on container port 8080. Published
  ports are limited to `127.0.0.1:5173` and `127.0.0.1:8080`.
- From the running web container, the dev root returned HTML containing
  `/@vite/client`, that HMR client entry returned successfully, and the web root
  returned the built application HTML. `/future/controller/path` returned the
  same HTML as the web root, verifying the direct deep-path SPA fallback.
- `make down` stopped and removed the dev/web containers and Compose network.
- `make ci` passed the existing documentation, ticket-tooling, actionlint,
  Hadolint, and committed-history Gitleaks checks plus ESLint, strict TypeScript,
  and production/web builds. No placeholder `npm test` command was added.

## Review

- Self-review covered the complete `origin/main...b8b7fe8` implementation and
  the acceptance evidence. It corrected Vite's container host allowance,
  registered the React Hooks ESLint plugin, pinned the Nginx image digest, and
  replaced an ineffective solution-level `tsc --noEmit` with `tsc --build`.
  The stricter check exposed and led to adding the missing Vite CSS module type
  declaration.
- Implementation used two bounded `ticket-worker` agents with non-overlapping
  app/tooling and Docker/Compose ownership. Integration, dependencies, Git, and
  final verification remained with the orchestrator.
- A fresh `ticket-reviewer` independently reviewed ticket 1 and commit
  `b8b7fe8` against `origin/main`. It found no actionable implementation defect.
  Its only P2 finding was the then-missing required completion record; this file
  addresses that finding. The reviewer noted the optional browser HMR display
  check as unavailable, not as a mandatory gap.
- No known actionable findings remain.

## Verification

- `make check` — passed in Docker after the final implementation fixes.
- `make build` — passed in Docker on commit `b8b7fe8`.
- `docker compose up -d --build dev web` — passed.
- Container-to-container HTTP checks for the dev HTML, `/@vite/client`, web
  HTML, and the deep SPA path — passed.
- `make down` — passed.
- `make ci` — passed in Docker on the implementation tree, including all
  existing repository checks and the new application checks/build.

## Limits and next actions

The optional Windows browser display and live HMR observation were not run in
this environment. They are not mandatory for ticket 1. No mandatory checks
remain pending; the final GitHub `Repository checks` result is recorded in the
Draft PR rather than this report.
