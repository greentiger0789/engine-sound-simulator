# Ticket 5 — implementation record

## Status

Status: verified

## Scope

- Ticket: [EngineConfig 検証と単気筒プリセット](../005-engine-config-and-single-preset.md)
- Reviewed implementation commits: `25f280a5c9ea7a05353c759d35a28032a05489aa`
  and review correction `56873635f28cc215779a4391a9bd2c28525be890`
- Added a browser-independent `EngineConfig` contract and strict parser with
  deterministic, path-based validation issues. Cylinder count is derived only
  from `cylinders.length`; the core accepts any nonempty count and an optional
  validator cap supports bounded UI callers.
- Added a four-stroke, 720-degree single-cylinder preset whose metadata labels
  every parameter as a synthetic model value rather than a real-engine
  measurement.
- Added engine config unit-test discovery to the existing Docker test and lint
  paths.

## Acceptance evidence

- `tests/engine/config.test.ts` parses the single-cylinder preset and checks its
  720-degree cycle, angle-zero cylinder, and model-value metadata.
- Validator tests reject unshaped and unknown input, non-finite numeric values,
  empty cylinders, duplicate cylinder IDs, out-of-range firing phases,
  non-positive inertia, invalid idle/redline ordering, and malformed torque
  curves. Issues include deterministic paths and explanatory messages; the
  parser copies valid values without clamping them.
- A two-cylinder fixture with distinct IDs and an identical firing angle parses
  successfully, preserving the future simultaneous-combustion contract.
- The default validator accepts five cylinders while the same input is rejected
  with `maxCylinders: 4`, keeping the core variable-length contract separate
  from an MVP UI limit.
- The engine test reads the module source and asserts that it has no React,
  browser DOM, or Web Audio dependency. Source inspection confirms that the
  only preset dependency is a type-only import of the engine contract.

## Review

- Self-review covered the full `origin/main...25f280a` implementation, including
  every mandatory rejection category, the half-open firing-angle interval,
  coincident angles, absence of silent normalization, deterministic issues,
  preset claims, test discovery, and the dependency-free module boundary.
- A bounded Terra medium `ticket-worker` implemented the engine contract,
  preset, and engine tests. The orchestrator retained shared test/lint config,
  Git, integration, review, and delivery ownership.
- A fresh Terra high `ticket-reviewer` independently reviewed the ticket, the
  full implementation diff, and this report. It found that the report claimed
  direct coverage of the preset's 720-degree cycle and angle-zero cylinder, but
  the test only asserted successful parsing and metadata. The correction adds
  explicit cycle, cylinder-count, and firing-angle assertions. The reviewer
  rechecked the correction, confirmed the finding resolved, and found no new
  actionable regression. No actionable findings remain.

## Verification

- `make test` — passed in Docker: 6 files, 34 tests, including 10 engine config
  tests and all 24 existing audio tests.
- `make check` — passed in Docker: Prettier, Markdownlint, ticket catalog and
  tooling tests, ESLint, and strict TypeScript compilation.
- `make build` — passed in Docker: application build and Nginx image.
- `make ci` — passed in Docker after the review correction, including check,
  34 unit tests, production builds, 12 Playwright tests across development and
  Nginx projects, actionlint, Hadolint, and committed-history Gitleaks.
- GitHub `Repository checks` is recorded in the Draft PR rather than this
  implementation record.

## Limits and next actions

No mandatory manual checks or review findings remain pending. The current-head
GitHub check is recorded in the Draft PR rather than this report.
