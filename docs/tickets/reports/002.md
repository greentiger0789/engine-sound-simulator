# Ticket 2 — implementation record

## Status

Status: manual-validation-pending

Independent review and final committed-tree CI are pending. All ticket-specific
automated acceptance checks have passed.

## Scope

- Ticket: [AudioWorklet の独立ビルドと境界契約](../002-worklet-build-contract.md)
- Reviewed implementation commit: `99f02ad7c05fb270ee086c330b8a7bce6eda7d5b`
- Added a separately emitted AudioWorklet entry, a shared dev/production URL
  resolver, typed processor messages and load failures, and the reserved
  `replace-config` boundary.
- Added the minimal silent processor with `throttle` and `gain` AudioParams,
  plus Dockerized Vitest and Playwright verification.

## Acceptance evidence

- Playwright Chromium received `ready` after a real user click for both the
  Vite development server and the Nginx production image. The exact module URLs
  returned successful JavaScript MIME responses.
- Vitest instantiated the processor at 44.1 kHz and 48 kHz and verified silence
  for output lengths 1, 127, 129, and 257 rather than assuming 128 frames.
- The production build emitted
  `dist/assets/engine-audio-worklet.js` as an executable entry containing the
  registered `engine-audio-processor`; the E2E uses `AudioWorklet`, not Worker.
- Unit tests verify successful loading and the discriminated
  `worklet-load-failed` result. Playwright verifies the same shared loader with
  an invalid module URL in Chromium.
- The test-only HTML remains under `tests/e2e` and was absent from the product
  `dist` listing.

## Review

- Self-review covered `origin/main...99f02ad`, including Worklet real-time
  constraints, Vite output, Docker isolation, typed boundaries, error paths,
  test relevance, and documentation. Integration fixes moved the processor
  name into a main-thread-safe contract module, used a native-compatible
  `AudioWorkletProcessor` constructor, typechecked test sources, and used
  trustworthy `.localhost` browser-test origins.
- Two `ticket-worker` agents implemented non-overlapping audio-contract and
  browser/build paths with Terra medium. Shared configuration, integration,
  Git, and final verification remained with the orchestrator.
- The independent Terra high reviewer found two actionable issues: the initial
  Nginx E2E bypassed the shared resolver/loader, and teardown failure was not
  reflected in `make e2e` status. The correction adds a production-mode harness
  under the non-root `/__e2e__/` base only in the derived E2E web image, runs
  both success and typed-error cases through the shared APIs, removes host port
  publication from the isolated test stack, and propagates cleanup failures.
  The reviewer rechecked the corrections and found both issues resolved with no
  new actionable regression.

## Verification

- `make check` — passed in Docker, including formatting, Markdown, ticket
  tooling, ESLint, and TypeScript for application/config/test sources.
- `make test` — passed: 3 Vitest files, 6 tests.
- `make build` — passed; the independent Worklet entry was emitted in the
  application and Nginx images.
- `make e2e` — passed in the isolated Compose project: 4 passed. Dev and web
  production-mode loading/ready plus shared-loader error scenarios all passed;
  the web resolver used the configured `/__e2e__/` base. The ordinary product
  web image still excludes the harness.
- `make e2e DOCKER_COMPOSE=/tmp/ess-e2e-compose-stub` with a stubbed successful
  start and exit-7 teardown — failed with exit 7 as required. Inspection after
  the normal run found no test project containers, volumes, or network.
- Final `make ci` and GitHub `Repository checks` — pending.

## Limits and next actions

The optional Windows Chromium/Edge observation was not run in this environment.
No sound-quality judgment is applicable because this processor intentionally
outputs silence. Complete independent review, final committed-tree CI, and the
Draft PR checks before changing the status to verified.
