# Ticket 20 — browser and long-run validation implementation record

## Status

Status: verified

The automated browser regressions and the hobby-stage Chrome owner smoke check
are complete. Cross-browser, ten-minute, profiler, and measured physical
latency evidence remains optional until public-release preparation.

## Scope

- Ticket: [実ブラウザ横断・長時間動作の検証](../020-browser-and-long-run-validation.md)
- Reviewed implementation revisions: `6e95420` and correction `c4efed4`.
- Added the [Windows browser and long-run audio validation runbook](../../browser-validation-runbook.md)
  for later public-release validation without making that release-grade matrix a
  dependency of ongoing hobby development.
- Kept automated Chromium coverage for the actual Vite and Nginx product paths.

## Acceptance evidence

- On 2026-09-22, the owner reported checking the application in Chrome and
  observing no problems. This is the required hobby-stage Chrome smoke
  acceptance. The OS, Chrome version, audio device, sample rate, exact
  scenarios, duration, and measured latency were not supplied and are not
  inferred.
- `tests/e2e/worklet.spec.ts` exercises the emitted product in Chromium through
  both Vite and Nginx. It covers successful Worklet start/stop, four-cylinder
  pending-configuration promotion, client-side invalid-phase validation, and
  typed failure/recovery UI paths.
- The owner explicitly made Edge and Firefox validation out of scope for the
  current hobby-development phase. Cross-browser testing, the four-cylinder
  48 kHz ten-minute run, CPU/memory profiling, and three measured audible-response
  trials are retained in the runbook as optional public-release preparation.
- No headless result is presented as proof of a physical sample rate, duration,
  device latency, or absence of audible dropouts.

## Review

- Self-review of the original delivery covered the complete
  `origin/main..6e95420` diff and its recording fields, sampling guidance,
  pass/fail semantics, production/development URLs, ten-minute checkpoints,
  response measurement, and separation of native and injected failures.
- A bounded Terra medium `ticket-worker` prepared the original runbook. A fresh
  Terra high `ticket-reviewer` found eight evidence and measurement ambiguities
  across its review rounds; all were corrected before the original merge.
- The 2026-09-22 policy revision preserves that runbook while separating future
  public-release evidence from the smaller owner acceptance required to continue
  hobby development.

## Verification

- Original focused Docker Prettier and Markdownlint checks — passed.
- Original `make test` — passed in Docker: 22 files and 168 tests.
- Original `make e2e` — passed in Docker: 30 Playwright tests across Vite
  development and Nginx production projects.
- Original `make ci` — passed in Docker on committed delivery-tree revision
  `c4efed4`, including formatting, Markdown, ticket tooling, ESLint, strict
  TypeScript, unit/offline tests, application and Nginx builds, Playwright,
  actionlint, Hadolint, and committed-history Gitleaks.
- Chrome owner smoke check — passed by owner report on 2026-09-22 with no
  problems reported; detailed environment and measurement values were not
  supplied.

## Limits and next actions

- This status accepts the current owner-driven hobby-development target. It is
  not a claim of Edge or Firefox compatibility, ten-minute stability, a 48 kHz
  physical run, a 50 ms audible response, profiler headroom, or production
  readiness.
- Before a public deployment is planned, execute the retained runbook at the
  desired browser/device matrix and record the detailed results without
  replacing missing measurements with estimates.
- No mandatory check remains pending for the current revised ticket contract.
