# Ticket 24 — drivetrain load and gear implementation record

## Status

Status: verified

The original implementation and keyboard-control follow-up passed their Docker gates and independent reviews. The optional manual observation has not been reported.

## Scope

- Ticket: [車両負荷・クラッチ・ギア拡張](../024-drivetrain-load-and-gear.md).
- Reviewed original implementation revision: `9cad6f9`. Reviewed keyboard-control follow-up revision: `c9de627`.
- Added a separate validated `DrivetrainConfig` and 1 kHz vehicle model without changing `EngineConfig`. Gear 0 and disengaged clutch contribute no vehicle torque; the existing dynamometer remains an independent load.
- Connected vehicle load and speed to the AudioWorklet, added gear/clutch controls and stopped-only mass/ratio editing, and displayed vehicle speed in km/h.
- Added ↑/↓ shifting in `1 → N → 2` order, temporary half-clutch on `C`, full disengagement on `Shift+C`, and restoration of the prior clutch setting on release or window blur. The default clutch setting is fully engaged while the initial gear remains neutral.

## Acceptance evidence

- `tests/engine/drivetrain.test.ts` covers neutral, disengaged and engaged coupling, increasing vehicle speed, lower coupled engine RPM, speed-dependent coasting resistance, fixed-step/audio-frame agreement, and invalid ratio/mass/control rejection.
- `tests/audio/engine-audio-processor.test.ts` compares product Worklet neutral and engaged gear at 48 kHz, asserting finite PCM, positive engaged vehicle speed and lower engaged RPM. `tests/audio/controller.test.ts` checks invalid mass/ratio rejection, gear/clutch transfer and restart state.
- `tests/e2e/worklet.spec.ts` exercises the actual Vite and Nginx product UI, including gear/clutch controls, live positive vehicle speed after throttle, invalid vehicle settings, a valid apply, start/stop and existing preset flows. The follow-up case checks keyboard gear order, both held clutch levels, effective override during slider changes, restoration on release/blur/hidden, and native input-field arrow handling.
- The complete `make test` run passed 25 files / 193 tests, covering the existing bike, two-stroke, V6 and V8 regressions. `make e2e` passed 34 Chromium tests across development and Nginx.

## Review

- Self-review covered the whole task diff, numerical boundaries, Worklet configuration validation, controller state and UI evidence. It corrected explicit null handling at the Worklet boundary and added a rejection test.
- A `gpt-6-sol` / medium worker owned `src/engine/drivetrain.ts` and its unit tests because numerical behavior and its coupling contract needed cross-module reasoning. A `gpt-6-luna` / high worker owned the bounded UI, CSS and E2E changes after the controller API was fixed. Both reported the selected model/effort and no unresolved implementation issue.
- A fresh `ticket-reviewer` (`gpt-6-sol` / high) reviewed the ticket, entire diff, report, model, Worklet, controller, UI, tests and documentation. It found a product E2E evidence gap: the previous speed assertion ran before audio start and proved only zero was finite. The E2E now polls for positive displayed speed after selecting gear/clutch, starting audio and applying throttle; both browser projects passed. The reviewer rechecked the correction and reported no remaining actionable code findings. It also identified an overbroad report acceptance claim, which was corrected to distinguish the passed tests from pending `make ci`.
- Follow-up self-review covered key repeats, modifier transitions, focus, hidden/blur cleanup, controller defaults and native control access. A fresh `ticket-reviewer` (`gpt-6-sol` / high) found that changing the slider while holding `C` could overwrite the effective half-clutch; the correction saves the new slider setting while keeping the held override, and E2E checks restoration after release. During verification, the first hidden-state handler added a second `visibilitychange` listener and failed the existing listener-count E2E in both browser projects. The correction uses the existing listener; the complete 36-test E2E suite then passed. The reviewer rechecked the latest handler and reported no remaining actionable findings.

## Verification

- `make test` — passed in Docker on the working tree after boundary correction: 25 files / 193 tests.
- `make e2e` — passed in Docker on the working tree: 34 tests across Vite development and Nginx.
- `make check` — passed in Docker after formatting, including Prettier, Markdownlint, catalog/tooling checks, ESLint and TypeScript.
- `make ci` — passed in Docker on committed implementation revision `9cad6f9`, including formatting, Markdown, catalog/tooling, ESLint, TypeScript, 193 unit tests, application and Nginx builds, 34 browser tests, actionlint, Hadolint and committed-history Gitleaks.
- Keyboard-control follow-up `make ci` — passed in Docker on committed revision `c9de627`, including formatting, Markdown, catalog/tooling, ESLint, TypeScript, 193 unit tests, application and Nginx builds, 36 browser tests, actionlint, Hadolint and committed-history Gitleaks.
- Final-head GitHub `Repository checks` — pending until the follow-up is pushed.

## Limits and next actions

- The vehicle parameters are synthetic model values. The one-way clutch transfer omits wheel-driven engine overrun and precise tire, road and transmission physics, as allowed by the ticket scope.
- Optional manual screen and listening observations have not been reported; no physical behavior is claimed.
- No mandatory local check or review finding remains pending. Current-head GitHub CI is tracked in the Draft PR.
